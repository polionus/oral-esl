# Rohingya Translation Interpreter - RAG-based
# Python 3.10+

# Pin NumPy <2 for scipy/sklearn compatibility (sentence-transformers stack)
numpy<2

# LLM client (OpenAI-compatible, works with Ollama)
openai>=1.0.0

# Vector store and embeddings
chromadb>=0.4.0
sentence-transformers>=2.2.0

# Evaluation (for eval_sentences.py)
openpyxl>=3.0.0
